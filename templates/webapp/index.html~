{% extends 'webapp/base.html' %}

{% load staticfiles %}

{% block title %}Index{% endblock %}

{% block body_block %}

    <div class="row placeholders">
    
        <div class="col-xs-12 col-sm-6 placeholder">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">Bands</h3>
                </div>
            </div>

            {% if bands %}
                <ul class="list-group">
                    {% for band in bands %}
                        <li class="list-group-item">{{ band.name }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <strong>There are no bands to show.</strong>
            {% endif %}
        </div>
        
        <div class="col-xs-12 col-sm-6 placeholder">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">Gigs</h3>
                </div>
            </div>

            {% if events %}
                <ul class="list-group">
                    {% for event in events %}
                     <li class="list-group-item">{{ event.name }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <strong>There are no gigs present.</strong>
            {% endif %}
        </div>
        
    </div>
{% endblock %} 